# Legacy Prisma workflow - disabled
# This workflow is no longer needed as the app uses JSON file storage
name: Database Migrations (Disabled)

on:
  # Disabled - no longer using Prisma
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest
    if: false  # Always skip this workflow

    steps:
    - name: Workflow Disabled
      run: echo "This workflow is disabled - app now uses JSON file storage"